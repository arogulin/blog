{% extends "index.twig" %}

{% block title %}{{ post.title }}{% endblock %}

{% block content %}
    <h1>{{ post.title }}</h1>
    <div>{{ post.date | date("d.m.Y H:i") }}</div>
    <div>
        {{ post.content | raw }}
    </div>
{% endblock %}